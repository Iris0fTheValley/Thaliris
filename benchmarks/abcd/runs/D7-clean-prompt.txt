Run the sealed D7 task in this repository. Begin with `context task-start "Shared Live2D runtime and renderer parity"` and then delegate all repository inspection and edits to a fresh managed child. The first child MUST be spawned with native agent_type="thaliris-investigator" and fork_turns="none"; do not omit that argument. Use one long native wait_agent, not polling. Investigator must keep its working set private, write a bounded repo-relative Evidence Artifact containing reusable facts/evidence refs/files/verification/unknowns/contradictions, and return only artifact path/index/status. Read that artifact via Controller commands, then if a material choice remains unresolved spawn exactly one fresh thaliris-reasoning-specialist with fork_turns="none" and selected Decision Context; it must return DECISION or NEED_EVIDENCE without broad repo investigation. If it requests evidence, close it, send a fresh investigator, register the artifact with `context task-artifact`, then spawn a fresh Sol. Accepted implementation conclusions must be explicit in state or Terra handoff. Spawn a fresh thaliris-implementer with fork_turns="none" to implement, use a fresh thaliris-reviewer for every review round, close one-shot Sol/Reviewers with native controls, and use long waits. Do not modify evaluator files or sealed tests. Finish implementation, focused tests, and review. Final message must be concise status plus artifact/evidence refs only, not the investigation body.
