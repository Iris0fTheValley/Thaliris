# D-run artifacts

Sanitized reports and bounded test outputs from the D6-D8 Thaliris benchmark
runs. Session JSONL, prompts, source trees, credentials, and machine-specific
paths are intentionally excluded. Paths in reports use `<screening-root>` or
`<benchmark-root>` placeholders.
