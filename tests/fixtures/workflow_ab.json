{
  "schema_version": 1,
  "workload": {
    "id": "bounded-routing-fixture",
    "goal": "Add one bounded routing behavior without a live native run."
  },
  "workflows": {
    "A": {
      "name": "mixed-context baseline",
      "steps": ["investigate", "reason", "implement", "verify", "review"]
    },
    "B": {
      "name": "isolated-router treatment",
      "steps": ["fresh investigator", "bounded status packet", "fresh implementer", "deterministic verification"]
    }
  },
  "metrics": {
    "native_run": {
      "status": "UNAVAILABLE",
      "reason": "No explicit live native Codex A/B run was supplied; this fixture must not fabricate one."
    },
    "static_contracts": ["fresh fork classification", "controller raw-data exclusion", "bounded artifact pointers"]
  }
}
